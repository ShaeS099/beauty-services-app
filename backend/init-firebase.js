#!/usr/bin/env node
/**
 * Legacy script kept for compatibility.
 *
 * For local development seeding, use:
 *   node scripts/seedEmulator.js
 *
 * For production seeding, prefer writing a one-off admin script
 * with a service account and explicit confirmation steps.
 */
console.log('Use: node scripts/seedEmulator.js');
process.exit(0);
